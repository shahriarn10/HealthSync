{
    "name": "healthsync-root",
    "version": "1.0.0",
    "description": "Root package for HealthSync project",
    "scripts": {
      "start": "npm run dev --prefix server & npm run dev --prefix client",
      "server": "npm run dev --prefix server",
      "client": "npm run dev --prefix client"
    }
  }